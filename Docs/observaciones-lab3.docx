¿Cuáles son los mecanismos de interacción (I/O: Input/Output) que tiene el view.py con el usuario?
- Los mecanismos input son el seleccionar un numero en la funcion printmenu en el view.py
dependiendo de lo que se busque y los mecanisos output son los que te muetsran el menu y 
luego te muetsran el resultado de lo que se introdujo. 

• ¿Cómo se almacenan los datos de GoodReads en el model.py?

- Se almacenan en un catalogo el cual es un diccionario que tiene 4 llaves
y que para cada llave tiene un lista como su valor.

• ¿Cuáles son las funciones que comunican el view.py y el model.py?

- En el view.py las funcion initcatalog y el loadcatalog llaman al controller.py
en donde esas funciones leen el archivo luego usan el model.py para añadir los
datos a la estructura.


Como dice la guía, estas respuestas están basadas únicamente en el archivo list.py:

• ¿Cómo se crea una lista?

En list.py se reciben los parámatros con las que se van crear la lista en la funcióon
newList. Esta función llama a otro módulo que se encarga de crear en si la lista.
Luego esta lista es retornada.

• ¿Qué hace el parámetro cmpfunction=None en la función newList()?
Este parámetro es la función de comparación de la lista. Esta igualada a None 
para que este sea su valor predeterminado, sin embargo en la asignaura dice que el usuario
debería de dar o una función de comparación o un key para crear la lista.

• ¿Qué hace la función addLast()?
En list.py se reciben los parámetros para añadir un elemento en la última posición
de una lista. Sin embargo también se llama a otro módulo para que haga en si la operación.

• ¿Qué hace la función getElement()?
En list.py se reciben los parámetros para retornar el elemento de una posición en una lista
 Sin embargo también se llama a otro módulo para que haga en si la operación.

• ¿Qué hace la función subList()?

En list.py se reciben los parámetros para retornar una sublista de la lista
 Sin embargo también se llama a otro módulo para que haga en si la operación.